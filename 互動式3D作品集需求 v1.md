# 需求：建立互動式 3D 作品集 - AI Proof

請根據我附上的履歷，為我建立一個互動式 3D 作品集（「互動式履歷」）。

## 概念

以低多邊形風格打造一座漂浮島嶼上的主題樂園。每個遊樂設施會開啟我履歷中的一個區塊：

🎡 摩天輪 = 職涯時間軸 · 🎢 雲霄飛車 = 專案與成果 ·  
🎪 馬戲團帳篷 = 有趣／古怪的點子 · 🌳 花園 = 寫作、反思與學習 ·  
🌍 地球儀 = 我曾居住、工作或造訪過的地方 · 🪦 墓園 = 失敗的專案

## 技術需求

- **單一自包含 HTML 檔案**，僅使用原生 JavaScript（不使用 TypeScript，也不需要建置步驟）。
- 使用 Three.js r160 UMD，並以下列方式載入：

```html
<script src="https://cdn.jsdelivr.net/npm/three@0.160.0/build/three.min.js"></script>
```

（r160 是最後一個提供 UMD build 的版本，因此 `THREE` 是全域物件，無須 ES modules。）

- 不使用外部模型、圖片或紋理。所有物件都必須以程式碼透過基本幾何體建立（方塊、圓錐、圓柱、管狀物、二十面體），並使用 `flatShading` 呈現低多邊形風格。可以使用 Canvas 紋理（例如地球儀地圖與遊樂園入口招牌）。
- 僅使用 Google Fonts：標題字型為 Sniglet 800，內文字型為 Atkinson Hyperlegible。
- 請將它發布為 artifact，並提供我連結。

## 場景

- **漂浮島嶼**：草地圓盤、泥土邊緣、下方不規則岩石圓錐、零散岩石，以及繞著島嶼運行的雲朵。
- 從中央廣場通往每個設施的步道；包含一座有水滴粒子的噴泉、路燈、入口大門（附有寫著我遊樂園名稱的 Canvas 紋理招牌），以及散落的樹木。
- 每個遊樂設施都必須在自己清楚註解的程式碼區塊中建造，並放置在島嶼周圍。
- **摩天輪**：兩道輪圈、輻條、保持直立的懸掛車廂，以及輪圈上的燈泡。
- **雲霄飛車**：封閉的 `CatmullRomCurve3` 迴圈、兩條以 `TubeGeometry` 製作的軌道與一條中央脊柱、實例化枕木與支柱；4 節車廂沿曲線行駛，並使用 `makeBasis(side, normal, tangent)` 定向。速度需有類物理效果：下坡加速、爬坡時減速至緩慢爬行。
- **馬戲團帳篷**：條紋圓錐與圓柱（透過每個面的頂點色彩上色條紋）、波浪垂飾、燈泡、一面頂點會飄動的旗幟，以及游標懸停或點擊時爆發的五彩紙屑。
- **花園**：會搖擺的樹、池塘、長椅、樹籬、實例化花朵、飄落的葉子，以及夜間的螢火蟲。
- **地球儀**：球體搭配 Canvas 世界地圖（由粗略的洲際多邊形繪製）、傾斜的經線環、帶小飛機的環繞點狀軌道，以及依照我履歷中每個地點之緯度／經度放置的紅色圖釘。初始旋轉角度要讓我的圖釘面向鏡頭。
- **墓園**：弧形尖樁柵欄、有泥土墳包的墓碑與十字架、枯樹、漂浮的幽靈、霧氣精靈，以及夜間帶有彩色光源的提燈。

## 互動

- **軌道相機**：拖曳旋轉（限制極角）、滾輪或雙指縮放；使用者 5 秒沒有輸入後，啟用柔和的待機搖擺。
- 點擊遊樂設施（針對不可見的代理網格進行 raycast，而非複雜幾何體）時，相機要飛往該設施並開啟側邊面板，顯示相應內容。使用 `camera.setViewOffset` 偏移渲染，讓設施在面板左側剩餘空間的中央；手機版則上移至底部面板上方。
- 在每個設施上方放置浮動 HTML 標籤，每一影格透過投影世界座標錨點定位。將它們設定為 `aria-hidden` 與 `tabindex="-1"`；底部 dock 才是可存取的版本。
- 懸停時，設施稍微上浮，並突顯其標籤。
- 支援 hash 路由（`#projects`、`#graveyard`），使各區塊能以連結分享；按 Esc 或關閉按鈕時，關閉面板。

## 介面與降級方案

- 提供一個「**Classic view**」，把所有區塊渲染為純文字的捲動頁面，並先顯示 About 區塊（摘要、技能、語言、電子郵件）。若 WebGL 或 Three.js 載入失敗，這也是自動降級方案。
- **日／夜按鈕**：天空漸層 shader、霧效、半球光與太陽光、發光燈泡、點光源、星星、月亮及煙火，都必須在兩種狀態間交叉淡變。
- **暫停按鈕**：停止所有環境動畫。
- 僅針對相機飛行、待機搖擺與煙火閃光遵守 `prefers-reduced-motion`；遊樂設施本身仍應持續運作，否則樂園看起來會壞掉。
- **夜空**：約 1,500 顆星星，以兩個閃爍圖層分布於整個球面（相機俯看島嶼，所以只在地平線上方放星星永遠看不見）；偶爾出現流星；月亮位於島嶼遠端邊緣上方；以及煙火——從島嶼邊緣發射的火箭，爆炸成牡丹、圓環與金色垂柳。每次煙火短暫以一個可重複使用的 PointLight 照亮樂園。夜間點擊空白天空時可發射更多煙火。
- **手機版**：標籤與面板要自適應；面板改為底部抽屜式面板，相機拉遠以容納畫面。

## 內容

請將**所有文字**放在 script 頂端的一個 `SECTIONS` 物件內，讓我可以在不修改 3D 程式碼的情況下編輯：

```js
const SECTIONS = {
  timeline:  { icon:'🎡', unit:'stops', title:'Timeline', kind:'timeline',
               items:[{ when, title, org, text }] },
  projects:  { icon:'🎢', unit:'projects', title:'Projects', kind:'projects',
               items:[{ title, org, text, tags:[], link }] },
  ideas:     { icon:'🎪', unit:'ideas', title:'Odd ideas', kind:'ideas',
               items:[{ status, title, text }] },
  garden:    { icon:'🌳', unit:'entries', title:'Garden', kind:'garden',
               items:[{ type, date, title, text }] },
  places:    { icon:'🌍', unit:'places', title:'Places', kind:'places',
               items:[{ flag, country, cities:[], note, coords:[lat, lon] }] },
  graveyard: { icon:'🪦', unit:'projects laid to rest', title:'Graveyard', kind:'graveyard',
               items:[{ title, years, cause, lesson }] }
};
```

請根據我的履歷填入內容：

- **時間軸**：所有職務與學位，按最新至最舊排序；包含日期、雇主與地點，以及重要獎項。
- **專案**：我最具代表性的工作，保留履歷中的真實數字與指標。新增技能標籤。
- **花園**：任何寫作或研究內容；否則請從我的摘要中整理簡短反思，以及證照。
- **地點**：我曾居住或工作過的所有地方，並提供用於地球儀圖釘的真實緯度／經度。
- **點子**與**墓園**：履歷不會涵蓋這些。請使用清楚標示的預留內容，並詢問我真正的內容。
- 在遊樂園招牌上放上我的姓名與職稱，並提供電子郵件與 LinkedIn 連結。
- 絕不可捏造關於我的事實。若資料缺失或有歧義，請先詢問，不要猜測。

## 視覺風格

使用溫暖的嘉年華調色盤：鮮紅色、芥末黃、奶油色、海軍藍墨色、數種綠色與天空青綠色。介面採用圓潤、粗獷的 UI，搭配實心偏移陰影與虛線分隔線。請使用 CSS 變數製作介面，使其能跟隨明暗模式切換。
